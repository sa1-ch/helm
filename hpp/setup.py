from setuptools import setup, find_packages

setup(
    name='hpp',
    version='1.0',
    url='',
    license='',
    author='dinesh.velmuruga',
    author_email='',
    packages=find_packages(),
    description='model deployment'
)
